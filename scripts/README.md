## scripts

These are the scripts used for data collection.  
Before running them, please make sure that the **config files have been copied to the corresponding directory in SPEC CPU 2017**, and that the `runcpu` command is executable in the current environment.
